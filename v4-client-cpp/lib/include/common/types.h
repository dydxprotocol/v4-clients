#pragma once

#include <string>
#include <vector>

namespace common {

using Symbol = std::string;
using Price = double;
using Quantity = double;
using BytesVec = std::vector<uint8_t>;

}  // namespace common
